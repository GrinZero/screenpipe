# i18n 多语言规范

本项目使用 **next-intl** 作为前端国际化框架，支持英文（默认）和简体中文。

## 目录结构

```
apps/screenpipe-app-tauri/
  i18n/
    config.ts          # locale 列表、默认 locale、标签
    request.ts         # next-intl server config（静态导出模式）
  messages/
    en.json            # 英文翻译（默认语言，必须最先更新）
    zh-CN.json         # 简体中文翻译
  lib/hooks/
    use-locale.ts      # locale 读写 hook
  components/
    i18n-provider.tsx  # NextIntlClientProvider 封装
    settings/
      language-settings.tsx  # 语言切换 UI
```

## 添加新翻译的流程

1. 在 `messages/en.json` 中添加 key（namespace 用点分隔层级）
2. 同步在 `messages/zh-CN.json` 中添加对应翻译
3. 组件中使用：

```tsx
import { useTranslations } from "next-intl";

function MyComponent() {
  const t = useTranslations("settings.general");
  return <h3>{t("autoStart")}</h3>;
}
```

## 翻译文件规则

- JSON key 使用 camelCase
- namespace 按页面/功能分组，例如 `settings.general`、`common`
- 带变量的文案用 `{variable}` 占位：`"runningVersion": "Running v{version}"`
- 组件中调用：`t("runningVersion", { version: "1.0" })`
- **两个语言文件的 key 必须完全一致**，缺失 key 会导致回退到 key 名称本身
- JSON 中不要使用中文引号 `""`，用 `「」` 或转义 `\"` 代替

## 添加新语言

1. 在 `i18n/config.ts` 的 `locales` 数组和 `localeLabels` 中添加新 locale
2. 在 `messages/` 下创建对应的 JSON 文件（如 `ja.json`）
3. 在 `components/i18n-provider.tsx` 的 `messageImports` 中添加动态 import

## 技术要点

- **纯客户端切换**：项目使用 `output: 'export'` 静态导出，没有服务端路由。locale 存储在 localStorage，切换时整页 reload
- **自动检测**：首次使用时优先读 localStorage → 再检测 `navigator.language` → 最终回退 `en`
- **避免闪烁**：`layout.tsx` 中有内联 script 在 paint 前设置 `<html lang>`
- **不要使用 locale 路由**：不需要 `[locale]` 路径段，Tauri 应用不走 URL 路由切换语言

## 严格单语规则

**页面上同一时刻只能出现一种语言的文案，严禁双语混排。** 除非用户明确要求双语显示，否则任何情况下都不允许一个页面中同时出现中英文 UI 文案。

具体要求：
- 迁移某个组件时，该组件内的 **所有** 用户可见文案必须一次性全部迁移，不能只翻译一半留另一半为英文
- 如果一个页面由多个组件组成，要么整页迁移，要么整页不迁移——不允许页面内一部分组件已翻译、另一部分还是硬编码英文
- Toast、Dialog、Tooltip 等弹出内容如果属于当前页面上下文，也必须一并迁移
- 未迁移的页面保持全英文（原样不动），这是可以接受的——只要单个页面内语言统一即可

## 迁移指南

现有组件中的硬编码字符串可以逐步迁移，不需要一次性改完。优先迁移用户可见的 UI 文案（标题、描述、按钮文字、toast 消息），内部日志和 debug 信息保持英文即可。

迁移前：
```tsx
<h3>Auto-start</h3>
<p>Start in the background when you log in</p>
```

迁移后：
```tsx
const t = useTranslations("settings.general");
<h3>{t("autoStart")}</h3>
<p>{t("autoStartDesc")}</p>
```
